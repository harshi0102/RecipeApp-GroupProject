<%= form_with model: @food, url: recipe_recipe_foods_path(@recipe), class: 'form', data: { turbo_frame: 'food_form' } do |form| %>
  <!-- form fields for food attributes -->
  <%= form.label :name %>
  <%= form.text_field :name %>
  <!-- Add other form fields here -->
  
  <%= form.submit 'Create Food' %>
<% end %>
